[project]
name = "endocrine-llm"
version = "0.1.0"
description = "Sistema de Neuromodulación Endocrina para LLMs"
authors = [{name = "Nicola Korff", email = "tu-email@example.com"}]
requires-python = ">=3.8"
dependencies = [
    "torch>=2.0.0",
    "transformers>=4.30.0",
    "sentence-transformers>=2.2.0",
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "seaborn>=0.12.0",
    "nltk>=3.8.0",
    "textblob>=0.17.0",
    "sacrebleu>=2.3.0",
    "rouge-score>=0.1.0",
    "tqdm>=4.65.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.4.0", "black>=23.0.0", "isort>=5.12.0"]

[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"